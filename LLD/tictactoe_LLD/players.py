from abc import ABC, abstractmethod

class Player:

    def __init__(self, player_id: int, player_move: int, player_score: int):
        self.player_id = player_id
        self.player_character = player_character
        self.player_score = self.player_score
